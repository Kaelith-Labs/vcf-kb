# .github/workflows/ is intentionally ignored until the gh OAuth token on
# this machine gets the `workflow` scope (run: gh auth refresh -s workflow).
.github/workflows/

node_modules
*.log
.DS_Store
.vitest-cache
